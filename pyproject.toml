[project]
name = "finger"
version = "0.1.0"
description = "wappalyzer-compatible fingerprint capability repository"
requires-python = ">=3.9"
dependencies = [
    "jsonschema>=4.21",
    "pyyaml>=6.0",
]

[tool.uv]
package = false
